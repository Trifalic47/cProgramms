#include <stdio.h>

int main() {
  // We should define our variable in the camle case format which means we
  // should keep our variable names like this ->
  int myAge;
  char firstLetter;
  float valuePi;
  // This style is callled the camle case variable naming format
  // And one more thing is that we should define the name of const variables in
  // upper case which shows the given variable is const. Example of naming an
  // const variable

  const int YEAR = 2026;

  return 0;
}
